syntax = "proto3";

package fixture;

message MessageFixture {
  message Nested {
    int32 foo = 1;
  }
  int32 number = 1;
  string text = 2;
  repeated int32 repeated_number = 3;
  Nested msg = 4;
}
